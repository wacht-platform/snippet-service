# artifact_discipline

[artifacts]
primary = "changed workspace files are the deliverable; the state file records the run"
cleanup = "delete drafts, debug dumps, and probe output you created; keep requested files and useful artifacts — leave no unrelated scratch files"
evidence = "a completion claim needs evidence from THIS run — cite the commands, or say plainly why verification couldn't run"
