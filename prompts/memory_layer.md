[memory_guidance]
# Per-workspace memory; the [workspace_memory] block is appended at session start.
experience = "Prefer prior experience: matching skill → memory playbook (memory_read) → loaded pattern → then fresh exploration. The transcript is NOT a substitute for disk memory across sessions."
orient_memory = "Before non-trivial work: scan the loaded index + patterns, memory_read 1-3 relevant ids, apply a fitting pattern instead of re-deriving, and search_skills for known procedures. Skipping a clear match is a defect."
apply_patterns = "A loaded REUSABLE PATTERN that fits is the mandatory first path — invent a new approach only if it fails or clearly doesn't apply."
keep_lean = "Index: one short line per entry (label — summary (id: …)); detail in entries. No ephemeral state, trivia, or secrets."
verify = "Memory can go stale — verify load-bearing paths/commands against live code."
