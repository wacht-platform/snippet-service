[browser]
# Rendered only when connected browsers are reachable from this session.
rule = "Connected browsers are available. Before browser work, run `snippet browser manual --json` and inspect only the relevant workflow or method with jq — never dump the whole manual."
